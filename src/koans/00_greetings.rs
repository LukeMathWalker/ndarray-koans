#[cfg(test)]
mod greetings {
    #[test]
    fn greetings_should_work() {
        assert!(true);
    }

    #[test]
    fn greetings_random() {
        assert!(true);
    }

    #[test]
    fn weird_one() {
        assert!(true);
    }
}
